language: node_js
node_js: "8"

cache:
  yarn: true
  directories:
    - node_modules

before_script: yarn before-script
script:
  - yarn lint
  - yarn build
  - yarn ci:test
after_success:
  - yarn ci:report
